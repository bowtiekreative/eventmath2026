// Generated by EventMath Compiler v0.4
// Runtime: <script src="eventmath-runtime.js"></script>

const EM = typeof EventMathRuntime !== 'undefined'
  ? EventMathRuntime
  : require('../runtime/eventmath-runtime.js');

// use directions from ../prediction/directions.em
const { directions } = require('./../prediction/directions.em.js');
// use lenses from ../prediction/lenses.em
const { lenses } = require('./../prediction/lenses.em.js');
// use quantities from ../prediction/quantities.em
const { quantities } = require('./../prediction/quantities.em.js');

let what_lens_accuracy = (function() {
  const _resolved = strategy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.lens === "what"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
let location_lens_accuracy = (function() {
  const _resolved = strategy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.lens === "where"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
let who_lens_accuracy = (function() {
  const _resolved = strategy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.lens === "who"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
let direct_accuracy = (function() {
  const _resolved = strategy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "direct"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
let indirect_other_accuracy = (function() {
  const _resolved = strategy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "indirect other"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
let total_accuracy = (function() {
  const _resolved = strategy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.resolved === true));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();

// note: Business strategy prediction
// note: Full cycle: generate predictions, resolve outcomes, score accuracy, find best lens
// note: Generate all 216 market entry predictions
// predict: "market entry decision" → strategy predictions
const strategy_predictions = new EM.EventMathLayer('strategy predictions', []);
directions.events.forEach(_dir => {
  lenses.events.forEach(_lens => {
    quantities.events.forEach(_qty => {
      strategy_predictions.events.push(new EM.EventMathEvent(
        'prediction_' + Date.now() + '_' + Math.random().toString(36).slice(2),
        'prediction',
        {
          subject: 'market entry decision',
          direction: _dir.matter ? _dir.matter.name : String(_dir.id),
          lens: _lens.matter ? _lens.matter.name : String(_lens.id),
          quantity: _qty.matter ? _qty.matter.name : String(_qty.id),
          predicted_state: (_dir.matter ? _dir.matter.name : '') + ' change through ' + (_lens.matter ? _lens.matter.name : '') + ' lens affecting ' + (_qty.matter ? _qty.matter.name : ''),
          confidence: 0.5,
          resolved: false,
          correct: null
        }
      ));
    });
  });
});

// note: Count predictions by lens to understand coverage
let people_factors = strategy_predictions.events.filter(evt => evt.matter.lens === "who").length;
let location_factors = strategy_predictions.events.filter(evt => evt.matter.lens === "where").length;
let timing_factors = strategy_predictions.events.filter(evt => evt.matter.lens === "when").length;
// note: Simulate resolution from past market entries
// resolve: strategy predictions
strategy_predictions.events = strategy_predictions.events.map(_p => {
  if ((_p.matter.direction === "direct") && (_p.matter.lens === "what")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: strategy predictions
strategy_predictions.events = strategy_predictions.events.map(_p => {
  if ((_p.matter.direction === "direct") && (_p.matter.lens === "where")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: strategy predictions
strategy_predictions.events = strategy_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect other") && (_p.matter.lens === "who")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: strategy predictions
strategy_predictions.events = strategy_predictions.events.map(_p => {
  if ((_p.matter.direction === "more same") && (_p.matter.lens === "when")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// resolve: strategy predictions
strategy_predictions.events = strategy_predictions.events.map(_p => {
  if ((_p.matter.direction === "keep same") && (_p.matter.lens === "why")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// resolve: strategy predictions
strategy_predictions.events = strategy_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect opposite") && (_p.matter.lens === "how")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// note: Score accuracy by lens to find which lens is most predictive
// note: Score accuracy by direction
// note: Overall resolved accuracy
console.log(strategy_predictions.render());

// ── Export for use ─────────────────────────────────
module.exports = {
  strategy_predictions,
};