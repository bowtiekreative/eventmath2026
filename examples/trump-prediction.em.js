// Generated by EventMath Compiler v0.4
// Runtime: <script src="eventmath-runtime.js"></script>

const EM = typeof EventMathRuntime !== 'undefined'
  ? EventMathRuntime
  : require('../runtime/eventmath-runtime.js');

// use directions from ../prediction/directions.em
const { directions } = require('./../prediction/directions.em.js');
// use lenses from ../prediction/lenses.em
const { lenses } = require('./../prediction/lenses.em.js');
// use quantities from ../prediction/quantities.em
const { quantities } = require('./../prediction/quantities.em.js');

let oil_pressure_from_iran_stalemate = 1;
let inflation_pressure_from_oil_spike = "oil pressure";
let voter_sentiment_from_inflation_pressure = "voter anger";
let donor_confidence_from_elite_panic = "dropping";
let iran_deal_chance;
let iran_collapse_chance;
let iran_stalemate_chance;
let inflation_eases_chance;
let inflation_worsens_chance;
let recession_risk_score;
let gop_hold_chance;
let dem_flip_chance;
let gop_expand_chance;
let trump_strong_score;
let trump_weak_score;
let trump_steady_score;
let iran_forecast = "iran stalemate";
let economy_forecast = "inflation worsens";
let midterm_forecast = "gop holds house";
let trump_forecast = "trump weakens";

// note: Trump 2026 — EventMath Prediction Model v2
// note: Next pass: feedback loops, pulse oscillation, populated zoom timelines
// note: June 11, 2026. Iran ceasefire collapsing. Inflation 4.2%.
// note: Midterms 5 months out. Trump at 80. World Cup incoming.
// note: 
// note: Five forces → causal chains → oscillating controls → prediction
// note: ═════════════════════════════════════════════════
// note: FORCE 1: IRAN WAR
// note: ═════════════════════════════════════════════════
// Event: "iran ceasefire holds"
const iran_ceasefire_holds = new EM.EventMathEvent(
  "iran ceasefire holds",
  "iran trajectory",
  {
    name: "Ceasefire Holds",
    description: "Deal closed within 2 weeks. Strait reopens.",
    probability: "low",
    "effect on oil": "drops",
    "effect on trump": "boost",
  }
);

// Event: "iran ceasefire collapses"
const iran_ceasefire_collapses = new EM.EventMathEvent(
  "iran ceasefire collapses",
  "iran trajectory",
  {
    name: "Ceasefire Collapses",
    description: "Full war. Kharg seized. Hormuz closed.",
    probability: "medium",
    "effect on oil": "catastrophic",
    "effect on trump": "crisis rally",
  }
);

// Event: "iran stalemate"
const iran_stalemate = new EM.EventMathEvent(
  "iran stalemate",
  "iran trajectory",
  {
    name: "Stalemate",
    description: "Strikes continue. No deal. No collapse.",
    probability: "high",
    duration: "indefinite",
    "effect on oil": "slow bleed",
    "effect on trump": "erosion",
  }
);

// note: ═════════════════════════════════════════════════
// note: FORCE 2: INFLATION / ECONOMY
// note: ═════════════════════════════════════════════════
// Event: "inflation eases"
const inflation_eases = new EM.EventMathEvent(
  "inflation eases",
  "economy trajectory",
  {
    name: "Inflation Eases",
    description: "Iran war ends. Oil drops. Prices cool.",
    probability: "low",
    "effect on voters": "relief",
    "effect on midterms": "neutral",
    "fed action": "hold",
  }
);

// Event: "inflation worsens"
const inflation_worsens = new EM.EventMathEvent(
  "inflation worsens",
  "economy trajectory",
  {
    name: "Inflation Worsens",
    description: "5%+ by October. Fed forced to hike.",
    probability: "medium",
    "effect on voters": "anger",
    "effect on midterms": "anti incumbent",
    "fed action": "rate hike",
  }
);

// Event: "stagflation"
const stagflation = new EM.EventMathEvent(
  "stagflation",
  "economy trajectory",
  {
    name: "Stagflation",
    description: "Inflation stays high. Growth stalls.",
    probability: "medium",
    "effect on voters": "despair",
    "effect on midterms": "landslide against",
    "political cost to GOP": "severe",
  }
);

// note: ═════════════════════════════════════════════════
// note: FORCE 3: MIDTERM ELECTIONS
// note: ═════════════════════════════════════════════════
// Event: "gop holds house"
const gop_holds_house = new EM.EventMathEvent(
  "gop holds house",
  "midterm outcome",
  {
    name: "GOP Holds House",
    description: "Republicans keep narrow majority",
    probability: "medium",
    "effect on trump": "protected",
    "gerrymander factor": "strong",
    "turnout driver": "base",
  }
);

// Event: "democrats win house"
const democrats_win_house = new EM.EventMathEvent(
  "democrats win house",
  "midterm outcome",
  {
    name: "Democrats Flip House",
    description: "Iran war + inflation swing voters",
    probability: "medium",
    "effect on trump": "investigations restart",
    "turnout driver": "anger",
  }
);

// Event: "gop expands majority"
const gop_expands_majority = new EM.EventMathEvent(
  "gop expands majority",
  "midterm outcome",
  {
    name: "GOP Expands Majority",
    description: "Iran deal success + economy improves",
    probability: "low",
    "effect on trump": "unchecked",
    "turnout driver": "enthusiasm",
  }
);

// note: ═════════════════════════════════════════════════
// note: FORCE 4: TRUMP POLITICAL POSITION
// note: ═════════════════════════════════════════════════
// Event: "trump strengthens"
const trump_strengthens = new EM.EventMathEvent(
  "trump strengthens",
  "political trajectory",
  {
    name: "Trump Strengthens",
    description: "Iran deal. Economy rallies. Approval up.",
    probability: "low",
    "approval rating": "48",
    "midterm effect": "coat tails",
  }
);

// Event: "trump weakens"
const trump_weakens = new EM.EventMathEvent(
  "trump weakens",
  "political trajectory",
  {
    name: "Trump Weakens",
    description: "War drags. Inflation hurts. Approval drops.",
    probability: "medium",
    "approval rating": "38",
    "midterm effect": "liability",
  }
);

// Event: "trump steady"
const trump_steady = new EM.EventMathEvent(
  "trump steady",
  "political trajectory",
  {
    name: "Trump Steady",
    description: "Base holds. Independents drift.",
    probability: "high",
    "approval rating": "42",
    "midterm effect": "neutral",
  }
);

// note: ═════════════════════════════════════════════════
// note: FORCE 5: WORLD CUP / SOFT POWER (July 2026)
// note: ═════════════════════════════════════════════════
// Event: "world cup opens"
const world_cup_opens = new EM.EventMathEvent(
  "world cup opens",
  "world cup phase",
  {
    name: "World Cup Opens",
    description: "July 2026. 48 nations. US hosts.",
    timing: "July 2026",
    "media focus": "sports",
    "trump spotlight": "high",
  }
);

// Event: "world cup distraction"
const world_cup_distraction = new EM.EventMathEvent(
  "world cup distraction",
  "world cup phase",
  {
    name: "World Cup Distraction",
    description: "Iran war fades from headlines during games",
    timing: "mid July 2026",
    "media focus": "sports",
    "trump spotlight": "managed",
  }
);

// Event: "world cup finals"
const world_cup_finals = new EM.EventMathEvent(
  "world cup finals",
  "world cup phase",
  {
    name: "World Cup Finals",
    description: "Championship game. Global audience billions.",
    timing: "late July 2026",
    "media focus": "climax",
    "trump spotlight": "ceremonial",
  }
);

// Event: "world cup boost"
const world_cup_boost = new EM.EventMathEvent(
  "world cup boost",
  "soft power",
  {
    name: "World Cup Boost",
    description: "Successful WC improves global standing",
    probability: "medium",
    "effect on trump": "positive coverage",
  }
);

// Event: "world cup backlash"
const world_cup_backlash = new EM.EventMathEvent(
  "world cup backlash",
  "soft power",
  {
    name: "World Cup Backlash",
    description: "Iran war overshadows WC. Protests.",
    probability: "medium",
    "effect on trump": "negative coverage",
  }
);

// note: ═════════════════════════════════════════════════
// note: FEEDBACK LOOP EVENTS — causal chains
// note: ═════════════════════════════════════════════════
// Event: "oil price spike"
const oil_price_spike = new EM.EventMathEvent(
  "oil price spike",
  "feedback",
  {
    name: "Oil Price Spike",
    cause: "iran stalemate",
    "effect on gas": "5 dollars plus",
    "effect on inflation": "upward pressure",
  }
);

// Event: "voter anger"
const voter_anger = new EM.EventMathEvent(
  "voter anger",
  "feedback",
  {
    name: "Voter Anger",
    cause: "inflation",
    "effect on midterms": "anti incumbent",
    intensity: "rising",
  }
);

// Event: "war fatigue"
const war_fatigue = new EM.EventMathEvent(
  "war fatigue",
  "feedback",
  {
    name: "War Fatigue",
    cause: "iran stalemate",
    "effect on approval": "erosion",
    "effect on turnout": "depressed",
  }
);

// Event: "elite panic"
const elite_panic = new EM.EventMathEvent(
  "elite panic",
  "feedback",
  {
    name: "Elite Panic",
    cause: "war plus inflation",
    "effect on gop": "donors withhold",
    "effect on dems": "fundraising spike",
  }
);

// note: ═════════════════════════════════════════════════
// note: LAYERS
// note: ═════════════════════════════════════════════════
// Layer: "iran scenarios"
const iran_scenarios = new EM.EventMathLayer("iran scenarios", [
  iran_ceasefire_holds,
  iran_ceasefire_collapses,
  iran_stalemate,
]);

// Layer: "economy scenarios"
const economy_scenarios = new EM.EventMathLayer("economy scenarios", [
  inflation_eases,
  inflation_worsens,
  stagflation,
]);

// Layer: "midterm outcomes"
const midterm_outcomes = new EM.EventMathLayer("midterm outcomes", [
  gop_holds_house,
  democrats_win_house,
  gop_expands_majority,
]);

// Layer: "trump positions"
const trump_positions = new EM.EventMathLayer("trump positions", [
  trump_strengthens,
  trump_weakens,
  trump_steady,
]);

// Layer: "world cup phases"
const world_cup_phases = new EM.EventMathLayer("world cup phases", [
  world_cup_opens,
  world_cup_distraction,
  world_cup_finals,
]);

// Layer: "world cup results"
const world_cup_results = new EM.EventMathLayer("world cup results", [
  world_cup_boost,
  world_cup_backlash,
]);

// Layer: "feedback loops"
const feedback_loops = new EM.EventMathLayer("feedback loops", [
  oil_price_spike,
  voter_anger,
  war_fatigue,
  elite_panic,
]);

// Layer: "all forces"
const all_forces = new EM.EventMathLayer("all forces", [
  iran_scenarios,
  economy_scenarios,
  midterm_outcomes,
  trump_positions,
  world_cup_phases,
  feedback_loops,
]);

// note: ═════════════════════════════════════════════════
// note: TIMELINES
// note: ═════════════════════════════════════════════════
// Timeline: "iran war"
const iran_war = new EM.EventMathTimeline("iran war");

// Present: iran scenarios
iran_war.log.push(...iran_scenarios.events);

// Timeline: "economy"
const economy = new EM.EventMathTimeline("economy");

// Present: economy scenarios
economy.log.push(...economy_scenarios.events);

// Timeline: "midterms"
const midterms = new EM.EventMathTimeline("midterms");

// Present: midterm outcomes
midterms.log.push(...midterm_outcomes.events);

// Timeline: "world cup"
const world_cup = new EM.EventMathTimeline("world cup");

// Future: world cup phases
world_cup.future.push(...world_cup_phases.events);

// note: ═════════════════════════════════════════════════
// note: PULSE — Oscillating controls (the Switch)
// note: ═════════════════════════════════════════════════
// Pulse: "fatigue oscillation" — oscillates every 1 tick(s)
let fatigue_oscillation_pulse = false; // current state (the nucleus)
let fatigue_oscillation_count = 0;     // tick counter
const fatigue_oscillation = {
  name: 'fatigue oscillation',
  every: 1,
  get state() { return fatigue_oscillation_pulse; },
  tick() {
    fatigue_oscillation_count++;
    if (fatigue_oscillation_count >= 1) {
      fatigue_oscillation_count = 0;
      fatigue_oscillation_pulse = !fatigue_oscillation_pulse; // nucleus appears / disappears
      EM.getDefaultTimeline().append(new EM.TimelineEntry('set', { key: 'fatigue oscillation', value: fatigue_oscillation_pulse }));
    }
  },
  reset() { fatigue_oscillation_pulse = false; fatigue_oscillation_count = 0; }
};
EM.getDefaultTimeline()._state.pulses = EM.getDefaultTimeline()._state.pulses || {};
EM.getDefaultTimeline()._state.pulses['fatigue oscillation'] = fatigue_oscillation;

// note: when fatigue oscillation state is true, the public is tired of the war
// note: when it is false, the shock of each new strike resets attention
// Pulse: "news cycle" — oscillates every 2 tick(s)
let news_cycle_pulse = false; // current state (the nucleus)
let news_cycle_count = 0;     // tick counter
const news_cycle = {
  name: 'news cycle',
  every: 2,
  get state() { return news_cycle_pulse; },
  tick() {
    news_cycle_count++;
    if (news_cycle_count >= 2) {
      news_cycle_count = 0;
      news_cycle_pulse = !news_cycle_pulse; // nucleus appears / disappears
      EM.getDefaultTimeline().append(new EM.TimelineEntry('set', { key: 'news cycle', value: news_cycle_pulse }));
    }
  },
  reset() { news_cycle_pulse = false; news_cycle_count = 0; }
};
EM.getDefaultTimeline()._state.pulses = EM.getDefaultTimeline()._state.pulses || {};
EM.getDefaultTimeline()._state.pulses['news cycle'] = news_cycle;

// note: media spotlight oscillates between Iran coverage and World Cup
// note: every 2 ticks the focus alternates
// note: ═════════════════════════════════════════════════
// note: FEEDBACK CAUSAL CHAIN
// note: Iran stalemate → oil spike → inflation worsens → voter anger
// note: ═════════════════════════════════════════════════
// note: ═════════════════════════════════════════════════
// note: PREDICTION MODEL
// note: 4 predictions × 216 combos = 864 scenarios
// note: ═════════════════════════════════════════════════
// predict: "iran war" → all iran predictions
const all_iran_predictions = new EM.EventMathLayer('all iran predictions', []);
directions.events.forEach(_dir => {
  lenses.events.forEach(_lens => {
    quantities.events.forEach(_qty => {
      all_iran_predictions.events.push(new EM.EventMathEvent(
        'prediction_' + Date.now() + '_' + Math.random().toString(36).slice(2),
        'prediction',
        {
          subject: 'iran war',
          direction: _dir.matter ? _dir.matter.name : String(_dir.id),
          lens: _lens.matter ? _lens.matter.name : String(_lens.id),
          quantity: _qty.matter ? _qty.matter.name : String(_qty.id),
          predicted_state: (_dir.matter ? _dir.matter.name : '') + ' change through ' + (_lens.matter ? _lens.matter.name : '') + ' lens affecting ' + (_qty.matter ? _qty.matter.name : ''),
          confidence: 0.5,
          resolved: false,
          correct: null
        }
      ));
    });
  });
});

// predict: "economy" → all economy predictions
const all_economy_predictions = new EM.EventMathLayer('all economy predictions', []);
directions.events.forEach(_dir => {
  lenses.events.forEach(_lens => {
    quantities.events.forEach(_qty => {
      all_economy_predictions.events.push(new EM.EventMathEvent(
        'prediction_' + Date.now() + '_' + Math.random().toString(36).slice(2),
        'prediction',
        {
          subject: 'economy',
          direction: _dir.matter ? _dir.matter.name : String(_dir.id),
          lens: _lens.matter ? _lens.matter.name : String(_lens.id),
          quantity: _qty.matter ? _qty.matter.name : String(_qty.id),
          predicted_state: (_dir.matter ? _dir.matter.name : '') + ' change through ' + (_lens.matter ? _lens.matter.name : '') + ' lens affecting ' + (_qty.matter ? _qty.matter.name : ''),
          confidence: 0.5,
          resolved: false,
          correct: null
        }
      ));
    });
  });
});

// predict: "midterms" → all midterm predictions
const all_midterm_predictions = new EM.EventMathLayer('all midterm predictions', []);
directions.events.forEach(_dir => {
  lenses.events.forEach(_lens => {
    quantities.events.forEach(_qty => {
      all_midterm_predictions.events.push(new EM.EventMathEvent(
        'prediction_' + Date.now() + '_' + Math.random().toString(36).slice(2),
        'prediction',
        {
          subject: 'midterms',
          direction: _dir.matter ? _dir.matter.name : String(_dir.id),
          lens: _lens.matter ? _lens.matter.name : String(_lens.id),
          quantity: _qty.matter ? _qty.matter.name : String(_qty.id),
          predicted_state: (_dir.matter ? _dir.matter.name : '') + ' change through ' + (_lens.matter ? _lens.matter.name : '') + ' lens affecting ' + (_qty.matter ? _qty.matter.name : ''),
          confidence: 0.5,
          resolved: false,
          correct: null
        }
      ));
    });
  });
});

// predict: "trump" → all trump predictions
const all_trump_predictions = new EM.EventMathLayer('all trump predictions', []);
directions.events.forEach(_dir => {
  lenses.events.forEach(_lens => {
    quantities.events.forEach(_qty => {
      all_trump_predictions.events.push(new EM.EventMathEvent(
        'prediction_' + Date.now() + '_' + Math.random().toString(36).slice(2),
        'prediction',
        {
          subject: 'trump',
          direction: _dir.matter ? _dir.matter.name : String(_dir.id),
          lens: _lens.matter ? _lens.matter.name : String(_lens.id),
          quantity: _qty.matter ? _qty.matter.name : String(_qty.id),
          predicted_state: (_dir.matter ? _dir.matter.name : '') + ' change through ' + (_lens.matter ? _lens.matter.name : '') + ' lens affecting ' + (_qty.matter ? _qty.matter.name : ''),
          confidence: 0.5,
          resolved: false,
          correct: null
        }
      ));
    });
  });
});

// note: ═════════════════════════════════════════════════
// note: RESOLVE — Granular per lens/quantity/direction
// note: This is the fix: resolve by multiple dimensions
// note: to get nuanced accuracy scores instead of all or nothing
// note: ═════════════════════════════════════════════════
// note: Iran: stalemate path validated via indirect other + when lens
// resolve: all iran predictions
all_iran_predictions.events = all_iran_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect other") && (_p.matter.lens === "when")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all iran predictions
all_iran_predictions.events = all_iran_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect other") && (_p.matter.lens === "why")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all iran predictions
all_iran_predictions.events = all_iran_predictions.events.map(_p => {
  if ((_p.matter.direction === "direct") && (_p.matter.quantity === "small")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all iran predictions
all_iran_predictions.events = all_iran_predictions.events.map(_p => {
  if ((_p.matter.direction === "keep same") && (_p.matter.lens === "who")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// resolve: all iran predictions
all_iran_predictions.events = all_iran_predictions.events.map(_p => {
  if ((_p.matter.lens === "what") && (_p.matter.quantity === "large")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// note: Economy: inflation path validated via more same + what lens
// resolve: all economy predictions
all_economy_predictions.events = all_economy_predictions.events.map(_p => {
  if ((_p.matter.direction === "more same") && (_p.matter.lens === "what")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all economy predictions
all_economy_predictions.events = all_economy_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect opposite") && (_p.matter.lens === "when")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all economy predictions
all_economy_predictions.events = all_economy_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect opposite") && (_p.matter.lens === "why")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all economy predictions
all_economy_predictions.events = all_economy_predictions.events.map(_p => {
  if ((_p.matter.direction === "keep same") && (_p.matter.lens === "who")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// resolve: all economy predictions
all_economy_predictions.events = all_economy_predictions.events.map(_p => {
  if ((_p.matter.direction === "direct") && (_p.matter.quantity === "small")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// note: Midterms: GOP hold resolved by lens (structural/who)
// resolve: all midterm predictions
all_midterm_predictions.events = all_midterm_predictions.events.map(_p => {
  if (_p.matter.lens === "who") {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all midterm predictions
all_midterm_predictions.events = all_midterm_predictions.events.map(_p => {
  if (_p.matter.lens === "what") {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// resolve: all midterm predictions
all_midterm_predictions.events = all_midterm_predictions.events.map(_p => {
  if (_p.matter.lens === "where") {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all midterm predictions
all_midterm_predictions.events = all_midterm_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect other") && (_p.matter.lens === "why")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all midterm predictions
all_midterm_predictions.events = all_midterm_predictions.events.map(_p => {
  if (_p.matter.direction === "keep same") {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// note: Trump: weakening validated by indirect opposite + who lens
// resolve: all trump predictions
all_trump_predictions.events = all_trump_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect opposite") && (_p.matter.lens === "who")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all trump predictions
all_trump_predictions.events = all_trump_predictions.events.map(_p => {
  if ((_p.matter.direction === "indirect opposite") && (_p.matter.lens === "why")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: true }));
  }
  return _p;
});
// resolve: all trump predictions
all_trump_predictions.events = all_trump_predictions.events.map(_p => {
  if (_p.matter.direction === "keep same") {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// resolve: all trump predictions
all_trump_predictions.events = all_trump_predictions.events.map(_p => {
  if ((_p.matter.direction === "direct") && (_p.matter.quantity === "small")) {
    return new EM.EventMathEvent(_p.id, _p.cat, Object.assign({}, _p.matter, { resolved: true, correct: false }));
  }
  return _p;
});
// note: ═════════════════════════════════════════════════
// note: SCORE
// note: ═════════════════════════════════════════════════
iran_deal_chance = (function() {
  const _resolved = all_iran_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "keep same"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
iran_collapse_chance = (function() {
  const _resolved = all_iran_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "direct"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
iran_stalemate_chance = (function() {
  const _resolved = all_iran_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "indirect other"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
inflation_eases_chance = (function() {
  const _resolved = all_economy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "keep same"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
inflation_worsens_chance = (function() {
  const _resolved = all_economy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "more same"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
recession_risk_score = (function() {
  const _resolved = all_economy_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "indirect opposite"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
gop_hold_chance = (function() {
  const _resolved = all_midterm_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "keep same"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
dem_flip_chance = (function() {
  const _resolved = all_midterm_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "direct"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
gop_expand_chance = (function() {
  const _resolved = all_midterm_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "more same"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
trump_strong_score = (function() {
  const _resolved = all_trump_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "direct"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
trump_weak_score = (function() {
  const _resolved = all_trump_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "indirect opposite"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
trump_steady_score = (function() {
  const _resolved = all_trump_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.direction === "keep same"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
// note: ═════════════════════════════════════════════════
// note: ZOOM — Populated with feedback loop events
// note: ═════════════════════════════════════════════════
// zoom in on iran stalemate and inflation worsens into election tipping point
const election_tipping_point = new EM.EventMathTimeline('election tipping point');
election_tipping_point.zoomLevel = ((iran_stalemate.zoomLevel || 1) + 1);
const _ctrl_matter_election_tipping_point = {
  from: iran_stalemate.id,
  to: inflation_worsens.id,
  zoom_level: election_tipping_point.zoomLevel,
  control: true,
  gap_description: 'between iran stalemate and inflation worsens'
};
Object.keys(iran_stalemate.matter || {}).forEach(k => { _ctrl_matter_election_tipping_point['from_' + k] = iran_stalemate.matter[k]; });
Object.keys(inflation_worsens.matter || {}).forEach(k => { _ctrl_matter_election_tipping_point['to_' + k] = inflation_worsens.matter[k]; });
election_tipping_point.append(new EM.TimelineEntry('control', new EM.EventMathEvent('ctrl_' + Date.now(), 'control', _ctrl_matter_election_tipping_point)));

// zoom out on timeline midterms as event november summary
const november_summary = new EM.EventMathEvent(
  'november summary',
  'summary',
  {
    source_type: 'timeline',
    source_name: 'midterms',
    zoom_level: Math.max(1, (midterms.zoomLevel || 1) - 1),
    entry_count: midterms.log.length,
    summary: midterms.render()
  }
);

// note: ═════════════════════════════════════════════════
// note: EQUAL AND OPPOSITE CONTROLS
// note: ═════════════════════════════════════════════════
// zoom opposite: equal and opposite control of "iran stalemate"
const iran_breakthrough_path = (() => {
  const __src = iran_stalemate;
  const __level = (__src && __src.zoomLevel) || 1;
  const __opp = new EM.EventMathTimeline('iran breakthrough path');
  __opp.zoomLevel = __level;
  __opp.opposite = true;
  __opp.oppositeOf = 'iran stalemate';
  __opp.polarity = -1;
  const __oppMatter = {
    type: 'opposite control',
    opposite_of: 'iran stalemate',
    zoom_level: __level,
    polarity: 'negative',
    description: 'equal and opposite force to ' + 'iran stalemate'
  };
  __opp.append(new EM.TimelineEntry('control', new EM.EventMathEvent('opp_ctrl_' + Date.now(), 'opposite control', __oppMatter)));
  return __opp;
})();

// zoom opposite: equal and opposite control of "inflation worsens"
const inflation_cools = (() => {
  const __src = inflation_worsens;
  const __level = (__src && __src.zoomLevel) || 1;
  const __opp = new EM.EventMathTimeline('inflation cools');
  __opp.zoomLevel = __level;
  __opp.opposite = true;
  __opp.oppositeOf = 'inflation worsens';
  __opp.polarity = -1;
  const __oppMatter = {
    type: 'opposite control',
    opposite_of: 'inflation worsens',
    zoom_level: __level,
    polarity: 'negative',
    description: 'equal and opposite force to ' + 'inflation worsens'
  };
  __opp.append(new EM.TimelineEntry('control', new EM.EventMathEvent('opp_ctrl_' + Date.now(), 'opposite control', __oppMatter)));
  return __opp;
})();

// zoom opposite: equal and opposite control of "democrats win house"
const gop_hold_path = (() => {
  const __src = democrats_win_house;
  const __level = (__src && __src.zoomLevel) || 1;
  const __opp = new EM.EventMathTimeline('gop hold path');
  __opp.zoomLevel = __level;
  __opp.opposite = true;
  __opp.oppositeOf = 'democrats win house';
  __opp.polarity = -1;
  const __oppMatter = {
    type: 'opposite control',
    opposite_of: 'democrats win house',
    zoom_level: __level,
    polarity: 'negative',
    description: 'equal and opposite force to ' + 'democrats win house'
  };
  __opp.append(new EM.TimelineEntry('control', new EM.EventMathEvent('opp_ctrl_' + Date.now(), 'opposite control', __oppMatter)));
  return __opp;
})();

// zoom opposite: equal and opposite control of "trump weakens"
const trump_strong_path = (() => {
  const __src = trump_weakens;
  const __level = (__src && __src.zoomLevel) || 1;
  const __opp = new EM.EventMathTimeline('trump strong path');
  __opp.zoomLevel = __level;
  __opp.opposite = true;
  __opp.oppositeOf = 'trump weakens';
  __opp.polarity = -1;
  const __oppMatter = {
    type: 'opposite control',
    opposite_of: 'trump weakens',
    zoom_level: __level,
    polarity: 'negative',
    description: 'equal and opposite force to ' + 'trump weakens'
  };
  __opp.append(new EM.TimelineEntry('control', new EM.EventMathEvent('opp_ctrl_' + Date.now(), 'opposite control', __oppMatter)));
  return __opp;
})();

// zoom meta: meta-control governing [iran stalemate, inflation worsens, election tipping point, feedback loops]
const governing_conditions = (() => {
  const __governs = [iran_stalemate, inflation_worsens, election_tipping_point, feedback_loops];
  const __maxLevel = Math.max(...__governs.map(s => (s && s.zoomLevel) || 1));
  const __meta = new EM.EventMathTimeline('governing conditions');
  __meta.zoomLevel = __maxLevel + 1;
  __meta.meta = true;
  __meta.governs = __governs;
  __meta.governsNames = ['iran stalemate', 'inflation worsens', 'election tipping point', 'feedback loops'];
  const __metaMatter = {
    type: 'meta control',
    governs_count: __governs.length,
    governs: ['iran stalemate', 'inflation worsens', 'election tipping point', 'feedback loops'].join(', '),
    zoom_level: __meta.zoomLevel,
    description: 'meta-control governing the governing controls'
  };
  __meta.append(new EM.TimelineEntry('control', new EM.EventMathEvent('meta_ctrl_' + Date.now(), 'meta control', __metaMatter)));
  return __meta;
})();

// note: ═════════════════════════════════════════════════
// note: WORLD CUP INTERFERENCE PATTERN
// note: World Cup timeline is parallel to Iran timeline
// note: They overlap — zoom between them shows the interference
// note: ═════════════════════════════════════════════════
// zoom in on iran war and world cup into media battle ground
const media_battle_ground = new EM.EventMathTimeline('media battle ground');
media_battle_ground.zoomLevel = ((iran_war.zoomLevel || 1) + 1);
const _ctrl_matter_media_battle_ground = {
  from: iran_war.name,
  to: world_cup.name,
  zoom_level: media_battle_ground.zoomLevel,
  control: true,
  gap_description: 'between iran war and world cup'
};
media_battle_ground.append(new EM.TimelineEntry('control', new EM.EventMathEvent('ctrl_' + Date.now(), 'control', _ctrl_matter_media_battle_ground)));

// note: ═════════════════════════════════════════════════
// note: VERDICT
// note: ═════════════════════════════════════════════════
// note: ── Display all prediction scores ──
{ const __sv = iran_deal_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('iran deal chance:', __sv); } }
{ const __sv = iran_collapse_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('iran collapse chance:', __sv); } }
{ const __sv = iran_stalemate_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('iran stalemate chance:', __sv); } }
{ const __sv = inflation_eases_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('inflation eases chance:', __sv); } }
{ const __sv = inflation_worsens_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('inflation worsens chance:', __sv); } }
{ const __sv = recession_risk_score; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('recession risk score:', __sv); } }
{ const __sv = gop_hold_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('gop hold chance:', __sv); } }
{ const __sv = dem_flip_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('dem flip chance:', __sv); } }
{ const __sv = gop_expand_chance; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('gop expand chance:', __sv); } }
{ const __sv = trump_strong_score; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('trump strong score:', __sv); } }
{ const __sv = trump_weak_score; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('trump weak score:', __sv); } }
{ const __sv = trump_steady_score; if (__sv !== null && __sv !== undefined && typeof __sv.render === 'function') { console.log(__sv.render()); } else { console.log('trump steady score:', __sv); } }
console.log(iran_war.renderSection('present'));
console.log(economy.renderSection('present'));
console.log(world_cup.renderSection('future'));
console.log(election_tipping_point.renderSection('present'));
console.log(media_battle_ground.renderSection('present'));
console.log(governing_conditions.renderSection('present'));
console.log(november_summary.render());

// ── Export for use ─────────────────────────────────
module.exports = {
  iran_ceasefire_holds,
  iran_ceasefire_collapses,
  iran_stalemate,
  inflation_eases,
  inflation_worsens,
  stagflation,
  gop_holds_house,
  democrats_win_house,
  gop_expands_majority,
  trump_strengthens,
  trump_weakens,
  trump_steady,
  world_cup_opens,
  world_cup_distraction,
  world_cup_finals,
  world_cup_boost,
  world_cup_backlash,
  oil_price_spike,
  voter_anger,
  war_fatigue,
  elite_panic,
  november_summary,
  iran_scenarios,
  economy_scenarios,
  midterm_outcomes,
  trump_positions,
  world_cup_phases,
  world_cup_results,
  feedback_loops,
  all_forces,
  all_iran_predictions,
  all_economy_predictions,
  all_midterm_predictions,
  all_trump_predictions,
  iran_war,
  economy,
  midterms,
  world_cup,
  election_tipping_point,
  iran_breakthrough_path,
  inflation_cools,
  gop_hold_path,
  trump_strong_path,
  governing_conditions,
  media_battle_ground,
};