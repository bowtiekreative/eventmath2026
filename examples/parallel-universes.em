note Parallel Universes — computed with zoom and prediction engine
note Every gap has structure. Every branch has a bridge.

event quantum split
category branch point
matter
  particle is electron
  property is spin
  state before is superposition
  branches created is 2
end
end

event spin up realized
category universe alpha
matter
  result is up
  probability is 0.5
  universe is alpha
end
end

event spin down realized
category universe beta
matter
  result is down
  probability is 0.5
  universe is beta
end
end

layer branch point
  quantum split
end

layer alpha branch
  spin up realized
end

layer beta branch
  spin down realized
end

timeline universe alpha
past
  branch point
end
present
  alpha branch
end
future
end
end

timeline universe beta
past
  branch point
end
present
  beta branch
end
future
end
end

note ZOOM 1 — between measurement and outcome lives a timeline
zoom in on quantum split and spin up realized into quantum transition
mark transition depth as zoom level of quantum transition
check transition depth is greater than 1

note ZOOM 2 — between two parallel universes lives a timeline
zoom in on timeline universe alpha and timeline universe beta into decoherence bridge
mark bridge depth as zoom level of decoherence bridge
check bridge depth is greater than 1

note ZOOM 3 — inside the bridge lives another timeline
zoom in on timeline universe alpha and timeline decoherence bridge into second layer bridge
mark second layer depth as zoom level of second layer bridge
check second layer depth is greater than 1

note Three levels deep — the fractal principle holds

zoom out on timeline universe alpha as event alpha summary
zoom out on timeline universe beta as event beta summary

use directions from ../prediction/directions.em
use lenses from ../prediction/lenses.em
use quantities from ../prediction/quantities.em

predict structure of reality
across directions
and lenses
and quantities
into reality predictions

resolve reality predictions where direction is direct and lens is what as correct
resolve reality predictions where direction is indirect opposite and lens is how as incorrect
resolve reality predictions where direction is keep same and lens is why as correct
resolve reality predictions where direction is indirect other and lens is where as correct

mark what accuracy as accuracy of reality predictions where lens is what
mark how accuracy as accuracy of reality predictions where lens is how
mark why accuracy as accuracy of reality predictions where lens is why
mark where accuracy as accuracy of reality predictions where lens is where

check what accuracy is 1
check how accuracy is 0

run universe alpha past
run universe alpha present
run universe beta present
run quantum transition present
run decoherence bridge present
run second layer bridge present
run alpha summary
run beta summary
