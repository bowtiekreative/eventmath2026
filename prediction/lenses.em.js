// Generated by EventMath Compiler v0.4
// Runtime: <script src="eventmath-runtime.js"></script>

const EM = typeof EventMathRuntime !== 'undefined'
  ? EventMathRuntime
  : require('../runtime/eventmath-runtime.js');

// note: The six lenses — which dimension of a situation to examine
// Event: "who lens"
const who_lens = new EM.EventMathEvent(
  "who lens",
  "lens",
  {
    name: "who",
    description: "examine through the people and stakeholders dimension",
    focus: "agent",
  }
);

// Event: "what lens"
const what_lens = new EM.EventMathEvent(
  "what lens",
  "lens",
  {
    name: "what",
    description: "examine through the object and substance dimension",
    focus: "object",
  }
);

// Event: "location lens"
const location_lens = new EM.EventMathEvent(
  "location lens",
  "lens",
  {
    name: "where",
    description: "examine through the location and context dimension",
    focus: "location",
  }
);

// Event: "timing lens"
const timing_lens = new EM.EventMathEvent(
  "timing lens",
  "lens",
  {
    name: "when",
    description: "examine through the timing and sequence dimension",
    focus: "timing",
  }
);

// Event: "why lens"
const why_lens = new EM.EventMathEvent(
  "why lens",
  "lens",
  {
    name: "why",
    description: "examine through the cause and motivation dimension",
    focus: "cause",
  }
);

// Event: "how lens"
const how_lens = new EM.EventMathEvent(
  "how lens",
  "lens",
  {
    name: "how",
    description: "examine through the method and mechanism dimension",
    focus: "method",
  }
);

// Layer: "lenses"
const lenses = new EM.EventMathLayer("lenses", [
  who_lens,
  what_lens,
  location_lens,
  timing_lens,
  why_lens,
  how_lens,
]);


// ── Export for use ─────────────────────────────────
module.exports = {
  who_lens,
  what_lens,
  location_lens,
  timing_lens,
  why_lens,
  how_lens,
  lenses,
};