// Generated by EventMath Compiler v0.4
// Runtime: <script src="eventmath-runtime.js"></script>

const EM = typeof EventMathRuntime !== 'undefined'
  ? EventMathRuntime
  : require('../runtime/eventmath-runtime.js');

// use directions from directions.em
const { directions } = require('./directions.em.js');
// use lenses from lenses.em
const { lenses } = require('./lenses.em.js');
// use quantities from quantities.em
const { quantities } = require('./quantities.em.js');

let prediction_subject = "unknown";
let total_predictions = 0;
let correct_predictions_tally = 0;
let engine_accuracy = 0;

// note: Prediction Engine — uses all three dimension files
// note: Subject: override this mark in your own program
// note: Generate all 216 combinations
// predict: "prediction subject" → all predictions
const all_predictions = new EM.EventMathLayer('all predictions', []);
directions.events.forEach(_dir => {
  lenses.events.forEach(_lens => {
    quantities.events.forEach(_qty => {
      all_predictions.events.push(new EM.EventMathEvent(
        'prediction_' + Date.now() + '_' + Math.random().toString(36).slice(2),
        'prediction',
        {
          subject: 'prediction subject',
          direction: _dir.matter ? _dir.matter.name : String(_dir.id),
          lens: _lens.matter ? _lens.matter.name : String(_lens.id),
          quantity: _qty.matter ? _qty.matter.name : String(_qty.id),
          predicted_state: (_dir.matter ? _dir.matter.name : '') + ' change through ' + (_lens.matter ? _lens.matter.name : '') + ' lens affecting ' + (_qty.matter ? _qty.matter.name : ''),
          confidence: 0.5,
          resolved: false,
          correct: null
        }
      ));
    });
  });
});

// Action: "score predictions"
function scorePredictions(
) {
  let resolved_total = all_predictions.events.filter(evt => evt.matter.resolved === "yes").length;
  let correct_total = all_predictions.events.filter(evt => evt.matter.correct === "yes").length;
  total_predictions = "resolved total";
  correct_predictions_tally = "correct total";
  if (resolved_total > 0) {
    new_accuracy = (function() {
  const _resolved = all_predictions.events.filter(_p => _p.matter.resolved === true && (_p.matter.resolved === "yes"));
  const _correct = _resolved.filter(_p => _p.matter.correct === true);
  return _resolved.length > 0 ? Math.round((_correct.length / _resolved.length) * 100) / 100 : 0;
})();
    engine_accuracy = "new accuracy";
  }
  return;
}


// ── Export for use ─────────────────────────────────
module.exports = {
  all_predictions,
  scorePredictions,
};