// Generated by EventMath Compiler v0.4
// Runtime: <script src="eventmath-runtime.js"></script>

const EM = typeof EventMathRuntime !== 'undefined'
  ? EventMathRuntime
  : require('../runtime/eventmath-runtime.js');

// note: The six directions — how something can change
// Event: "direct direction"
const direct_direction = new EM.EventMathEvent(
  "direct direction",
  "compass",
  {
    name: "direct",
    description: "same cause produces same effect in same direction",
    polarity: "positive",
  }
);

// Event: "indirect opposite direction"
const indirect_opposite_direction = new EM.EventMathEvent(
  "indirect opposite direction",
  "compass",
  {
    name: "indirect opposite",
    description: "cause produces effect through inversion",
    polarity: "negative",
  }
);

// Event: "indirect other direction"
const indirect_other_direction = new EM.EventMathEvent(
  "indirect other direction",
  "compass",
  {
    name: "indirect other",
    description: "cause produces effect through a different path",
    polarity: "lateral",
  }
);

// Event: "keep same direction"
const keep_same_direction = new EM.EventMathEvent(
  "keep same direction",
  "compass",
  {
    name: "keep same",
    description: "no change current state persists",
    polarity: "neutral",
  }
);

// Event: "more same direction"
const more_same_direction = new EM.EventMathEvent(
  "more same direction",
  "compass",
  {
    name: "more same",
    description: "current state amplified",
    polarity: "amplified",
  }
);

// Event: "less same direction"
const less_same_direction = new EM.EventMathEvent(
  "less same direction",
  "compass",
  {
    name: "less same",
    description: "current state diminished",
    polarity: "diminished",
  }
);

// Layer: "directions"
const directions = new EM.EventMathLayer("directions", [
  direct_direction,
  indirect_opposite_direction,
  indirect_other_direction,
  keep_same_direction,
  more_same_direction,
  less_same_direction,
]);


// ── Export for use ─────────────────────────────────
module.exports = {
  direct_direction,
  indirect_opposite_direction,
  indirect_other_direction,
  keep_same_direction,
  more_same_direction,
  less_same_direction,
  directions,
};