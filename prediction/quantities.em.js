// Generated by EventMath Compiler v0.4
// Runtime: <script src="eventmath-runtime.js"></script>

const EM = typeof EventMathRuntime !== 'undefined'
  ? EventMathRuntime
  : require('../runtime/eventmath-runtime.js');

// note: The six quantities — how much of something is involved
// Event: "one quantity"
const one_quantity = new EM.EventMathEvent(
  "one quantity",
  "quantity",
  {
    name: "one",
    description: "exactly one instance or unit",
    scope: "singular",
  }
);

// Event: "partial quantity"
const partial_quantity = new EM.EventMathEvent(
  "partial quantity",
  "quantity",
  {
    name: "partial",
    description: "some but not all",
    scope: "subset",
  }
);

// Event: "many quantity"
const many_quantity = new EM.EventMathEvent(
  "many quantity",
  "quantity",
  {
    name: "many",
    description: "a large number of instances",
    scope: "plural",
  }
);

// Event: "any quantity"
const any_quantity = new EM.EventMathEvent(
  "any quantity",
  "quantity",
  {
    name: "any",
    description: "one or more regardless of which",
    scope: "existential",
  }
);

// Event: "none quantity"
const none_quantity = new EM.EventMathEvent(
  "none quantity",
  "quantity",
  {
    name: "none",
    description: "zero instances or absence",
    scope: "empty",
  }
);

// Event: "all quantity"
const all_quantity = new EM.EventMathEvent(
  "all quantity",
  "quantity",
  {
    name: "all",
    description: "the complete set without exception",
    scope: "universal",
  }
);

// Layer: "quantities"
const quantities = new EM.EventMathLayer("quantities", [
  one_quantity,
  partial_quantity,
  many_quantity,
  any_quantity,
  none_quantity,
  all_quantity,
]);


// ── Export for use ─────────────────────────────────
module.exports = {
  one_quantity,
  partial_quantity,
  many_quantity,
  any_quantity,
  none_quantity,
  all_quantity,
  quantities,
};